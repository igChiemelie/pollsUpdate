// .on('uploadImg',  (name, file) => {
    //     file.path = __dirname + '/uploads/' + file.name;    
    //     updateUserArr.uploadImg = value;
    // })
    // .on('file',  (name, file) => {
    // })